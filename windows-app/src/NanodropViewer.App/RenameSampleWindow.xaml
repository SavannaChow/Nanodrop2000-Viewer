<Window x:Class="NanodropViewer.App.RenameSampleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename sample"
        Width="420"
        Height="150"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#FFF8FAFC">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Update the selected sample name and save later as a new TBWK file."
                   TextWrapping="Wrap"
                   Foreground="#FF4E5B75" />

        <TextBox x:Name="SampleNameTextBox"
                 Grid.Row="1"
                 Margin="0,12,0,0"
                 MinWidth="320" />

        <StackPanel Grid.Row="2"
                    Margin="0,16,0,0"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Rename"
                    Width="88"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="HandleRenameClick" />
            <Button Content="Cancel"
                    Width="88"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
